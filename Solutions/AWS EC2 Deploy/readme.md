# Simple AWS EC2 deploy with remote state

This demonstrates a simple EC2 deployment using a Ubuntu 24.04 LTS AMI on a `t2.micro` EC2 instance. Additionally, the state is stored in S3 configured in the `terraform` block.